_schema-version: "3.2"
ID: project101
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: project101-destination-content
  type: com.sap.application.content
  requires:
  - name: project101-destination-service
    parameters:
      content-target: true
  - name: project101-repo-host
    parameters:
      service-key:
        name: project101-repo-host-key
  - name: project101-uaa
    parameters:
      service-key:
        name: project101-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: project101_html_repo_host
          ServiceInstanceName: project101-html5-service
          ServiceKeyName: project101-repo-host-key
          sap.cloud.service: project101
        - Authentication: OAuth2UserTokenExchange
          Name: project101_uaa
          ServiceInstanceName: project101-xsuaa-service
          ServiceKeyName: project101-uaa-key
          sap.cloud.service: project101
        existing_destinations_policy: update
  build-parameters:
    no-source: true
- name: project101-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: project101-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - project101.zip
      name: project101
      target-path: resources/
- name: project101
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: project101-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: project101-destination-service
    service-plan: lite
- name: project101-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: project101-xsuaa-service
    service-plan: application
- name: project101-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: project101-html5-service
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
